server:
  port: 8800
logging.level:
  root: INFO
  org.springframework.web: INFO
  org.hibernate: INFO
command:
  datasource.url: "jdbc:mysql://localhost:3306/axonexample_command"
  datasource.username: root
  datasource.password: rootpass
query:
  datasource.url: "jdbc:mysql://localhost:3306/axonexample_query"
  datasource.username: root
  datasource.password: rootpass